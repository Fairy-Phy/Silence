// DBテンプレート

module.exports =
    (message, date, who, cmd, args, connection) => {
        try {

        }
        catch (err) {
            console.log(`${cmd}コマンド実行中にエラーが発生しました\n${err}`);
        }
    };